{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "scripts": {
    "bootstrap": "./node_modules/.bin/lerna bootstrap",
    "build": "./node_modules/.bin/lerna run build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
